node_modules/
.next/
out/
build/

# env + db — ignore EVERY env file (incl. .env.production which holds LIVE
# Stripe keys); the committed templates end in .example and are re-included.
.env
.env.*
!.env*.example
prisma/dev.db
prisma/dev.db-journal

# generated
src/generated/
next-env.d.ts
tsconfig.tsbuildinfo

# misc
.DS_Store
*.log
uploads/
# editing-session scratch copies — safe to delete
*.fresh
